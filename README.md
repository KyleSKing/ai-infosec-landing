# Generated content branch

This branch stores generated article Markdown and archive indexes for `ai-infosec-landing`.

Source branches (`main` and `develop`) should not track `_posts/` or `archive/`.

GitHub Actions restores this branch before generation, builds `_site`, then saves updated generated content back here.
